import { InactivoDirective } from './inactivo.directive';

describe('InactivoDirective', () => {
  it('should create an instance', () => {
    const directive = new InactivoDirective();
    expect(directive).toBeTruthy();
  });
});
